{$DOMAIN} {
	@api path /api /api/*

	handle @api {
		reverse_proxy backend:8000 {
			# Stream SSE responses without proxy buffering.
			flush_interval -1
		}
	}

	handle {
		root * /srv
		try_files {path} /index.html
		file_server
	}
}
